# GEMINI Updates

## Directives
- **Prototype Conversion**: When converting from any single-page prototype (e.g., `@prototype/pantau-pendidikan-v14 March 2026.jsx`) to the actual React JS codebase:
  - Adjust code parts accordingly to fit the existing app's structure.
  - **Do NOT** change the code architecture.
  - Make sure that the visual layout strictly follows the prototype.

## Recent Updates
- **2026-03-11**: Converted `pantau-demo.mov` to `pantau-demo.mp4` in the `resources/` directory using `ffmpeg` to improve web compatibility and reduce file size.

